# DESIGN.md — 송연지 포트폴리오 블로그 디자인 시스템

이 문서는 디자인의 "단일 진실 공급원(Single Source of Truth)"입니다.
UI/스타일 관련 의사결정은 항상 이 문서를 우선합니다.

---

## 1. 컨셉

- **한 줄 정의**: "프로젝트를 카드로 한눈에 보는 프론트엔드 포트폴리오"
- **성격**: 취업 어필 · 역량 전시 · 활동 기록 융합
- **핵심 경험**: 상단 프로필(자격증·기술스택)과 경험·활동 리스트 → 그 아래 **프로젝트 카드 그리드**.
  카드를 클릭하면 **7섹션 상세**(소개·개요·진행한 일·과정·결과물·성장·역량)가 반응형으로 열린다.
- **톤 앤 무드**: 밝고 깔끔한 배경 위 따뜻한 노랑–갈색 조합. 종이 노트 같은 차분함.
- **이전 버전**: 달력(월 그리드)형. 카드 그리드형으로 전환됨([`REDESIGN_PLAN.md`](./REDESIGN_PLAN.md) 참고).

---

## 2. 컬러 팔레트

| 역할 | 이름 | HEX | RGB | 용도 |
|------|------|-----|-----|------|
| 포인트(연노랑) | `--color-accent` | `#fff9b0` | 255,249,176 | 강조 배경, 상태/분류 배지, 기술 칩 |
| 강조(갈색) | `--color-brand` | `#965f37` | 150,95,55 | 버튼, 링크, 보더, 섹션 번호 |
| 텍스트(다크브라운) | `--color-ink` | `#4b2814` | 75,40,20 | 본문/제목 텍스트, 진한 강조 |
| 배경 | `--color-bg` | `#fffdf7` | — | 페이지 배경(아주 옅은 크림) |
| 카드 배경 | `--color-surface` | `#ffffff` | — | 카드/상세 표면 |
| 보조 텍스트 | `--color-muted` | `#9c8472` | — | 캡션, 요약, 메타 정보 |
| 보더 | `--color-line` | `#ece3d6` | — | 카드/구분선 |

규칙
- 본문 텍스트는 항상 `--color-ink`. 회색(`#333` 등) 임의 사용 금지.
- 갈색(`--color-brand`)은 "상호작용 가능" 신호로만 사용(링크/버튼/포커스).
- 연노랑(`--color-accent`)은 면적을 넓게 쓰지 말고 포인트로만(배지·칩 등).
- 명도 대비(WCAG AA) 확보: `--color-ink` on `--color-bg`, 흰 텍스트 on `--color-brand`.

---

## 3. 아이콘

- **아이콘 라이브러리**: Font Awesome 6 (Free, CDN로 로드 — `index.html`).
- 이모지(📅)나 텍스트 이모티콘은 **사용하지 않는다.** 모든 아이콘은 Font Awesome로.
- 실제 사용 아이콘: `fa-xmark`(상세 닫기), `fa-folder-open`(빈 상태), `fa-certificate`(자격증),
  `fa-arrow-up-right-from-square`(데모), 브랜드 아이콘(`fa-github`, `fa-react`, `fa-js` 등 기술 칩).
- 아이콘 색은 맥락 색을 상속(`currentColor`). 단독 강조는 `--color-brand`.

---

## 4. 레이아웃

### 4.1 전체 구조 (위 → 아래)
1. **About** — 역할 · 이름(송연지) · 한 줄 소개 · GitHub 링크 · **자격증 배지** · **기술 스택 칩**.
2. **경험 · 활동(Experience)** — 비프로젝트(연구실/대외활동/동아리) 컴팩트 리스트(정적).
3. **프로젝트(Project Grid)** — 프로젝트 카드 **반응형 그리드**(데스크톱 3열).
4. **작성 폼(NewPostForm)** — **개발 환경에서만** 노출(배포본 없음).
5. **상세(PostModal)** — 카드 클릭 시 7섹션을 모달/모바일 하단 시트로 표시.
6. **Footer** — 카피라이트 + 데이터 출처.

### 4.2 카드 그리드 규칙
- 메인 그리드는 **프로젝트만**(`category === '프로젝트'`).
- 반응형: 데스크톱 **3열** / 태블릿(≤860) 2열 / 모바일(≤600) 1열.
- 카드 높이는 행 기준으로 정렬, 기술 칩은 카드 하단 정렬(`margin-top:auto`).
- 0개일 때: 점선 박스 + `fa-regular fa-folder-open` + "아직 등록된 프로젝트가 없습니다."

### 4.3 카드(요약형)
표시 항목: 상태 배지(완료/진행중) · 제목(18px) · 한 줄 요약(`summary`, 2줄 clamp) · 기술 칩(최대 3 + `+N`).
(메인은 전부 프로젝트라 카테고리 배지는 두지 않는다.)

### 4.4 상세 (PostModal)
- 헤더(sticky): 카테고리 배지 · 상태 배지 · 기간(`formatPeriod`) · 닫기 버튼(`fa-solid fa-xmark`).
- 본문(스크롤): 제목 · 요약 · 기술 칩 · 링크(데모/GitHub) · **7섹션**(번호+제목+본문, `white-space: pre-line`).
- 섹션이 없으면 `content` 폴백, 둘 다 없으면 "본문이 준비 중입니다".
- 닫기: 배경 딤 클릭 / ESC / 닫기 버튼. 포커스 트랩 + 트리거로 포커스 복원.
- 모바일(≤600): 하단 풀스크린 시트(slide-up, `prefers-reduced-motion` 대응).

### 4.5 경험·활동 / 작성 폼
- **경험·활동**: 각 항목 = 카테고리 배지(`categories.js` 매핑) + 제목/요약 + 기간. 클릭 없음(정적).
- **작성 폼(dev)**: 점선 카드. meta(제목·요약·상태·시작/종료일·기술·링크) + 7섹션 textarea. `intro` 필수.

---

## 5. 데이터 ↔ 화면 매핑

`posts` 테이블 1행 = 활동 1건. 프로젝트는 그리드 카드, 비프로젝트는 경험 리스트로 간다.

| 필드 | 화면 위치 |
|------|-----------|
| `title` | 카드/상세/경험 제목 |
| `summary` | 카드 한 줄 요약 · 상세 요약 |
| `sections` (JSONB) | 상세 7섹션 본문(객체 `{intro..skills}`) |
| `content` | (레거시) 상세 폴백. 신규는 `sections` 사용 |
| `activity_date` / `end_date` | 상세·경험의 기간(`formatPeriod`). 진행중이면 `end_date` NULL |
| `category` | 분리 키(프로젝트=그리드 / 그 외=경험) + 배지 |
| `tech_stack` | 기술 칩 |
| `demo_url`, `repo_url` | 상세 링크 버튼 |
| `status` | 상태 배지 (완료/진행중) |

> 자격증·기술 스택은 DB가 아니라 `data/profile.js` 고정값으로 About에 표시한다.

### 카테고리 배지 색 가이드 (분류 5종)
- 프로젝트: 갈색 채움(brand) — `badge--project`
- 자격증: 연노랑 채움 + 다크브라운 텍스트 — `badge--cert`
- 연구실: 다크브라운 채움 + 흰 텍스트 — `badge--lab`
- 대외활동: 갈색 아웃라인(보더만) — `badge--activity`
- 동아리: 연노랑 채움 + 갈색 보더 — `badge--club`

---

## 6. 타이포그래피

- 폰트: 시스템 한글 산세리프 우선
  (`Pretendard`가 있으면 사용, 없으면 `-apple-system, "Apple SD Gothic Neo", "Noto Sans KR", sans-serif`).
- 스케일
  - 페이지 타이틀(이름): 32–40px / 700
  - 섹션 타이틀(프로젝트/경험): 20–24px / 700
  - 카드 제목: 18px / 700
  - 본문: 15–16px / 1.6 line-height
  - 캡션·메타: 13px / `--color-muted`
- 한국어 가독성을 위해 `word-break: keep-all` 적용(어절 단위 줄바꿈).

---

## 7. 간격 · 모서리 · 그림자

- 간격 스케일(px): 4 · 8 · 12 · 16 · 24 · 32 · 48.
- 모서리: 카드 `12px`, 배지/칩 `999px`(pill), 버튼 `8px`.
- 그림자: 약하게. `0 4px 12px rgba(75,40,20,0.06)` (잉크색 기반 그림자로 톤 통일).
- 컨테이너 최대 폭: `960px`, 좌우 패딩 모바일 `16px` / 데스크톱 `24px`.

---

## 8. 상태(State) 디자인

- **로딩**: 중앙 텍스트 "프로젝트를 불러오는 중…" + 차분한 색.
- **에러**: 빨강 대신 `--color-brand` 톤으로 부드럽게, 재시도 안내.
- **프로젝트 0건**: 점선 박스 + 폴더 아이콘 + 안내 문구.
- **호버**: 카드 떠오름(translateY -4px) + 그림자 강화.
- **포커스**: 키보드 포커스 링 `--color-brand` 2px. 접근성 필수.

---

## 9. 접근성(A11y)

- 모든 인터랙티브 요소(카드, 상세 닫기, 작성 폼)는 키보드 접근 가능.
- 아이콘 단독 버튼에는 `aria-label`.
- 색만으로 의미 전달 금지 — 카테고리/상태는 텍스트 라벨 동반.
- 상세 열림 시 포커스 트랩 + ESC 닫기 + 트리거로 포커스 복원.

---

## 10. 하지 말 것 (Don'ts)

- 이모지/이모티콘 사용 금지(아이콘은 Font Awesome로).
- 팔레트 외 임의 색 추가 금지.
- 한 화면에 연노랑 면적 과다 사용 금지(눈부심).
- 인라인 스타일 남발 금지 — 색/간격은 CSS 변수 사용.
